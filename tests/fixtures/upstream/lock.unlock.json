{
  "method": "lock.unlock",
  "note": "synthetic — trigger ack (vendor-unverified; shape from src/lock.js observation, all fields optional except action). Captured for shape-pinning only; the live canary does not call write ops.",
  "sample": {
    "action": "unlock",
    "code": 0,
    "success": true,
    "message": "ok",
    "op": "cmd",
    "data": {}
  }
}
